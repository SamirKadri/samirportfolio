---
import type { HeroProps } from "@types";
type Props = HeroProps;

const { name, specialty, summary, email } = Astro.props;
---

<section class="py-20 md:py-32" id="#hero">
  <div class="flex flex-col gap-8 opacity-0 animate-slide-in animation-delay-300">
    <h1 class="font-serif text-5xl font-bold text-neutral-800 sm:text-6xl md:text-7xl">
      Hello! 👋
    </h1>
    
    <p class="font-serif text-3xl leading-relaxed text-neutral-600 sm:text-4xl sm:leading-relaxed md:text-5xl md:leading-tight">
      I'm <span class="font-bold text-neutral-900">{name}</span>, a {specialty} at UofT who {summary} 🚀
    </p>

    <div class="mt-4 flex items-center gap-2 text-lg sm:text-xl">
      <span class="text-neutral-500">Get in touch 👉</span>
      <a
        class="font-medium text-primary hover:text-primary/80 transition-colors border-b-2 border-primary/20 hover:border-primary"
        href={`mailto:${email}`}
      >
        {email}
      </a>
    </div>
  </div>
</section>
